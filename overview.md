# 一人公司工作台 · 交付说明

## 成果

| 项目 | 内容 |
|---|---|
| 本地文件 | `solo-workbench.html`（单文件，可直接双击打开） |
| 部署方式 | 任意静态托管即可（GitHub Pages / 本地服务器）；PWA 版见 `solo-pwa/` |
| 数据存储 | 浏览器本地持久化 + JSON 导出/导入备份 + 流水 CSV 导出 |

> 说明：本文件为内部交付记录，对外公开版本已移除私有链接与环境信息。

## 工作台结构

左栏（PC）/ 顶部（手机）：**长期工作** 主线（预置内容已清空，由你自己填），每项挂里程碑，带进度条与逾期天数；下方是导航、数据备份区。
主区 6 个板块：

1. **今日总览** — 置顶「今天要处理」，逾期里程碑自动顺延到今天并标红，一键完成；含逾期数/今日待办/喝水进度/今日支出四项指标与本周节奏柱状图。**待办清单预置内容已清空**，空态给出引导。
2. **AI 资讯** — 7 条快照，每条提炼「关键信息 + 一人公司怎么用」，可存为选题或转为待办；右上角「更新资讯」可粘贴替换。
3. **抖音热榜** — 8 条热点，每条配一个契合选题与切入角度，可一键存入「我的选题库」或转待办。
4. **已学习**（本次新增）— 自由搭建的学习知识库，见下方专节。
5. **喝水打卡** — 进度环 + 杯子格子 + 近 7 天柱状图，可设每日目标与提醒间隔，超时页面内弹提醒（含「喝了 / 稍后」）。
6. **记账理财** — 收支录入（支出红、收入绿）、月预算进度条、本月支出饼图、近 6 个月收支双柱对比、流水明细；支持 CSV 导出。

## 「已学习」板块说明（本次新增）

**两级结构，都支持无限叠加：**

- **模块** = 一个学习主题（如「AI 工具与工作流」「财务知识」）。可自由新增、重命名、删除、上下移动顺序、折叠收起。
- **内容块** = 模块里的内容单元，一个模块可以叠加任意多个、任意多种，顺序随时可调。6 种类型：

| 类型 | 用途 |
|---|---|
| 文本笔记 | 一段话写清学到了什么 |
| 要点清单 | 多条要点，可逐条打勾标记「已掌握」 |
| 链接收藏 | 原文 / 教程 / 课程地址，点击直达 |
| 代码·提示词 | 可复用的代码片段与 prompt，等宽字体显示 |
| 金句摘录 | 摘抄原文并标注出处 |
| 图片 | 截图、手写笔记照片，自动压缩到 800px 宽存本地 |

顶部四张统计卡：模块数、内容块总数、要点掌握进度（已勾选/总数）、最近更新日期。
空态提供「建第一个模块」与「载入示例模块看看」两个入口，示例模块里叠了 4 种内容，看完可一键删除。
每个模块还有一个「+ 加入复习待办」，把复习动作推进左侧长期工作里。

## 本次预置的实时数据（2026-09-03 快照）

**AI 资讯要点**：OpenAI Astra 达 Critical 网安阈值改为受限发布；AI token 价格年内腰斩（指数降至 97 美分）；Claude Fable 5.1 缓存读取成本降 75%；Gemini 3.8 Flash + Flash Cyber 发布；Meta Muse Spark 1.3 编码超 GPT-5.6 Sol 且定价不变；谷歌 TPU 迭代提速至一年两款；可灵估值约 1228 亿元。

**抖音热榜与选题**：直播税务新规（10/1 起到账即扣税）→ 自由职业者扣税与现金流；台风沙德尔 → 居家办公协作流程；金价失守 4300 美元 → 一人公司要不要拿闲钱买金；手机涨价潮 → 设备更换决策表；央视抽查 304 钢印造假 → 日用品避坑清单；班级群求关照 → 如何体面拒绝白嫖；泰国取消 60 日免签 → 数字游民政策 Plan B；小米 18 Fold → 发布会前决策清单。

> 数据获取说明：AI HOT 官方接口与本地 DailyHotApi 服务在本次环境均不可达（外网策略拦截 / 服务未启动），上述内容经公开检索取得并如实标注来源，非 AI HOT 接口直出。页面内已提供「更新资讯 / 更新热榜」入口，可随时替换成最新数据。

## 技术要点

- 单文件 HTML，CSS/JS/图标/图表全内联，零外部依赖，断网可用。
- 图表为手写内联 SVG（进度环、柱状图、饼图、周节奏），图标为内联 SVG path，无 emoji。
- 渲染层 10 个函数互不调用，统一由 `refreshAll()` 调度；已学习里的 `buildModule` / `buildBlock` 是纯字符串构建函数，不碰 DOM，杜绝循环调用导致的栈溢出。
- 响应式：<860px 单列 + 底部 Tab，按钮 ≥44px、输入框 16px，适配 iPhone 底部安全区。
- localStorage key 统一 `wb_solo_` 前缀，避免与其它页面冲突。已学习用独立 key `wb_solo_learn`，导出/导入/清空均已覆盖。
- 旧版预置示例数据做了**一次性迁移清除**（标记 key `wb_solo_seeded2`），只删预置的 3 条示例长期工作，你自己录入的数据不受影响。

## 本次改动自检

用 Node 建了轻量 DOM 桩把脚本完整跑了 27 项断言，全部通过：初始化无异常、空态渲染、6 种内容块渲染、模块/内容块的增删改与排序、旧示例数据迁移、localStorage 持久化。

## 后续可做

- 接入资料库数据表实现多设备实时同步（当前为本地存储 + 手动备份）。
- 迭代加模块：每次加 1 个（如客户 CRM、周报生成器、发票与税负测算）。
